import { TodosInfoPipe } from './todos-info-pipe';

describe('TodosInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new TodosInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
