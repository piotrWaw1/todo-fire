import { CompletedCountPipe } from './completed-count-pipe';

describe('CompletedCountPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedCountPipe();
    expect(pipe).toBeTruthy();
  });
});
